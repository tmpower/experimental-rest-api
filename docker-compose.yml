# docker compose up

version: '3'

services:
  redis:
    # build: ./df-redis
    image: redis:alpine
    command: ["redis-server", "--appendonly", "yes"]
    restart: always
    ports:
      - "6379:6379"
    # networks:
    #   - appnet: {}
    # volumes:
    #   - redis-data:/data

  postgres:
    # build: ./df-postgres
    image: postgres:11
    restart: always
    # networks:
    #   - appnet: {}
    # volumes:
    #   - postgres-data:/var/lib/postgresql/data

  api:
    build: .
    restart: always
    ports:
      - "5000:5000"
    links:
      - postgres
      - redis
    depends_on:
      - postgres
      - redis
    # networks:
    #   - appnet: {}
  
  # nginx:
  # 	image: ./df-nginx
  # 	restart: always
  # 	ports:
  # 	 - "8080:8080"
  # 	networks:
  # 	 - appnet

  # ui:
  # 	image: .
  # 	links:
  # 		- api
  # 	ports:
  # 		- 5000:5000

  # volumes:
  #   postgres-data:
  #   redis-data: {}

  # networks:
  #   appnet:
  #     external: true